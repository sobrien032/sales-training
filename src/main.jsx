import React from "react";
import ReactDOM from "react-dom/client";
import QuirkTrainingLogin from "./App.jsx";
import "./index.css"; // Optional if you create one later

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <QuirkTrainingLogin />
  </React.StrictMode>
);
